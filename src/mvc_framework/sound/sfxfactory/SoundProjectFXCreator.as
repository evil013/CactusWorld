package mvc_framework.sound.sfxfactory{	import com.dayvid.mvc.sound.soundfx.SoundFXCreator;		import mvc_framework.sound.SoundFXList;	import com.dayvid.mvc.sound.soundfx.sfx_components.SoundFX;
	// Encapsulates SFX creation	public class SoundProjectFXCreator extends SoundFXCreator	{		private var embedSound:Object;				public function SoundProjectFXCreator(aModel, aEmbedSound:Object)		{			super(aModel);						embedSound = aEmbedSound;		}				override protected function createSound(aType:uint):Class 		{			if (aType == SoundFXList.CASHREGISTER) 			{				return embedSound.CashRegister;			} 			else if (aType == SoundFXList.SWOOSH) 			{				return embedSound.Swoosh;			} 			else if (aType == SoundFXList.BOTTOMBACKGRND) 			{				return embedSound.BottomBackGrnd;			} 			else if (aType == SoundFXList.MESSAGEPOP) 			{				return embedSound.MessagePop;			} 			else if (aType == SoundFXList.WRENCH) 			{				return embedSound.MechanicClank;			} 			else if (aType == SoundFXList.STEAM) 			{				return embedSound.Steam;			} 			else if (aType == SoundFXList.FIGHT) 			{				return embedSound.MechanicFight;			} 			else if (aType == SoundFXList.SNORE) 			{				return embedSound.Snoring;			} 			else if (aType == SoundFXList.TICKTOCK) 			{				return embedSound.ClockTickTock;			} 			else if (aType == SoundFXList.HMMMMM) 			{				return embedSound.Hmmm;			} 			else if (aType == SoundFXList.GULP) 			{				return embedSound.Gulp;			} 			else if (aType == SoundFXList.REPAIRMANUAL) 			{				return embedSound.RepairManual;			} 			else if (aType == SoundFXList.OBJECTDROPED) 			{				return embedSound.ObjectDroped;			} 			else if (aType == SoundFXList.BUTTONCLICKED) 			{				return embedSound.ButtonClicked;			} 			else if (aType == SoundFXList.WHISTLE) 			{				return embedSound.Whistle;			} 			else if (aType == SoundFXList.REVVING) 			{				return embedSound.Revving;			} 			else if (aType == SoundFXList.GUN) 			{				return embedSound.Gun;			} 			else if (aType == SoundFXList.CHEER) 			{				return embedSound.Cheer;			} 			else if (aType == SoundFXList.CROWD) 			{				return embedSound.Crowd;			} 			else if (aType == SoundFXList.APPLAUSELOOP) 			{				return embedSound.ApplauseLoop;			}			else if (aType == SoundFXList.BUTTONUPCLICKED) 			{				return embedSound.ButtonUpClicked;			}			else if (aType == SoundFXList.BUTTONDOWNCLICKED) 			{				return embedSound.ButtonDownClicked;			}			else if (aType == SoundFXList.REWARD_COLLECTED) 			{				return embedSound.RewardCollected;			}			else if (aType == SoundFXList.REWARD_HEALTH_RECHARGE) 			{				return embedSound.RewardHealthRecharge;			}			else if (aType == SoundFXList.DAMAGE_EXPLOSION) 			{				return embedSound.DamageExplosion;			}			else if (aType == SoundFXList.EXPLOSION) 			{				return embedSound.Explosion;			}			else if (aType == SoundFXList.TRACK_ANNOUNCER_START) 			{				return embedSound.TrackAnnouncerStartClass;			}			else if (aType == SoundFXList.MOTORCYCLE_OFF_TRACK) 			{				return embedSound.MotorcycleOffTrack;			}			else if (aType == SoundFXList.MOTORCYCLE_COLLISION) 			{				return embedSound.MotorcycleCollision;			}			else if (aType == SoundFXList.MOTORCYCLE_IMPLOSION) 			{				return embedSound.MotorcycleImplosion;			}			else if (aType == SoundFXList.RACE_FINISHED_EXPLOSION) 			{				return embedSound.RaceFinishExplosion;			}			else if (aType == SoundFXList.MOTORCYCLE_RACE_ENGINE) 			{				return embedSound.MotorcycleRaceEngine;			}			else if (aType == SoundFXList.FIREWORKS) 			{				return embedSound.Fireworks;			}			else if (aType == SoundFXList.SHOP_CONFUSED) 			{				return embedSound.ShopConfused;			}			else if (aType == SoundFXList.SHOP_FIGHT) 			{				return embedSound.ShopFight;			}			else if (aType == SoundFXList.SHOP_BREAKROOM) 			{				return embedSound.ShopBreakroom;			}			else if (aType == SoundFXList.SHOP_SLEEPING_1) 			{				return embedSound.ShopSleeping1;			}			else if (aType == SoundFXList.SHOP_SLEEPING_3) 			{				return embedSound.ShopSleeping3;			}			else if (aType == SoundFXList.SHOP_SLEEPING_4) 			{				return embedSound.ShopSleeping4;			}			else if (aType == SoundFXList.SHOP_MECH_EXIT) 			{				return embedSound.ShopMechExit;			}			else if (aType == SoundFXList.SHOP_CRY_BABY) 			{				return embedSound.ShopCryBaby;			}			else if (aType == SoundFXList.SHOP_NEED_PART) 			{				return embedSound.ShopNeedPart;			}			else if (aType == SoundFXList.SHOP_STOP_GOOFING_OFF) 			{				return embedSound.ShopStopGoofingOff;			}			else if (aType == SoundFXList.END_OF_RACE_ANN) 			{				return embedSound.EndOfRaceAnn;			}			else if (aType == SoundFXList.POWER_UP_BOMB) 			{				return embedSound.PowerUpBomb;			}			else if (aType == SoundFXList.POWER_UP_TIME) 			{				return embedSound.PowerUpTime;			}			else 			{				return super.createSound(aType);			}		}	}}